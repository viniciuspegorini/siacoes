# Dev local (padrao): app no Docker + PostgreSQL na maquina host
BUILD_MODE=dev
DB_SERVER=localhost:5432

# Stack completa com PostgreSQL no Docker:
# docker compose --profile with-db up --build
# BUILD_MODE=production
# DB_SERVER=postgres:5432
# POSTGRES_PORT=5433
